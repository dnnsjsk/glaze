{
  "name": "glaze-monorepo",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf docs/.next || true",
    "build": "npm run prebuild && npx turbo run build"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.15.1"
}
